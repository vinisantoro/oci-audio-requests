# OIDC SSO Configuration for OCI Domains
# Copy this file and configure with your OCI Domain OIDC settings

# ============================================================================
# OCI Domain Configuration
# ============================================================================
# URL base do domínio OCI Identity Domain
# Formato: https://<domain-id>.identity.oraclecloud.com
# Ou: https://<tenant>.idcs.oci.oraclecloud.com
OCI_DOMAIN_URL=https://<domain-id>.identity.oraclecloud.com

# ============================================================================
# OAuth2/OIDC Client Configuration
# ============================================================================
# Client ID da aplicação OIDC registrada no OCI Domain
# Obtenha em: OCI Console > Identity & Security > Domains > [Seu Domain] > Applications > [Sua App] > Configuration
CLIENT_ID=your-client-id-here

# Client Secret da aplicação OIDC
# IMPORTANTE: Mantenha este valor seguro e nunca o commite no repositório
CLIENT_SECRET=your-client-secret-here

# URL de callback OIDC
# Deve corresponder ao "Custom Social Linking Callback URL" e "Redirect URIs" configurados no OCI
# IMPORTANTE: Use /api/auth/callback, não /callback ou /login
CALLBACK_URL=https://notes.dailybits.tech/api/auth/callback

# ============================================================================
# Session Configuration
# ============================================================================
# Secret para assinar cookies de sessão
# Gere um valor aleatório seguro (ex: openssl rand -base64 32)
SESSION_SECRET=your-session-secret-here-change-this

# ============================================================================
# Application URLs (for OCI Domain Application Configuration)
# ============================================================================
# Application URL: https://notes.dailybits.tech
# Custom Sign-In URL: https://notes.dailybits.tech/api/auth/login
# Custom Sign-Out URL: https://notes.dailybits.tech/api/auth/logout
# Custom Social Linking Callback URL: https://notes.dailybits.tech/api/auth/callback
# Redirect URIs (OAuth Settings): https://notes.dailybits.tech/api/auth/callback

# ============================================================================
# OCI Object Storage Configuration (existing)
# ============================================================================
OCI_UPLOAD_URL=https://objectstorage.region.oraclecloud.com/p/par-id/n/namespace/b/bucket/o/

# ============================================================================
# Optional: Email Allowlist (extra security layer)
# ============================================================================
# Se configurado, apenas estes emails poderão acessar mesmo após autenticação OIDC
# Formato: JSON array string
# ALLOWED_EMAILS=["user1@example.com","user2@example.com"]
